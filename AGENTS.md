# AGENTS.md · 本项目协作规程（进入本目录即生效）

本项目由两个 AI 协作：**Hermes**（需求 / 思考 / 验收）与 **MiMoCode**（实现 / 测试）。
**作者（人类）是唯一裁决者。**

---

## 1. 通信：一律 JSON

- 双方往返消息用 JSON：`{ "from", "to", "round", "kind", ... }`。
- 不用自然语言小作文；结论、理由、动作需求各自成字段，便于机器复核。
- 异议写进 `DECISIONS.md` 的争议节，不靠消息堆叠。

## 2. 讨论：最多三轮

- 同一议题最多三轮往返。
- **三轮仍不一致 → 原样上报作者（人类）定夺**，不许自己拍板后继续。
- 宁可记录「未达成一致 + 双方方案」，**不许为取得一致而循环**。

## 3. 必须互相质问（不是各说各话）

- 提方案的一方必须给出**可验证的依据**；另一方**必须先挑战，再决定采纳或驳回**。
- 质问要具体：「这个断言怎么验证？」「这个设计在 X 情况下会怎样？」「这条规则的例外是什么？」
- 驳回要带理由；接受驳回要写明**采纳的是哪一条论据**，不许含糊同意。
- **鼓励自我批评**：主动指出自己交付物里最弱的一环。

## 4. 隐私：不留作者任何个人信息

- 用户名 / 真实盘符路径 / 机器名一律占位符化：`<USER>`、`%USERPROFILE%`、`<PROJECT_DIR>`、`<HOME>`。
- 提交或发布前做一次**全仓扫描**：真实用户名、`DESKTOP-*`、`%USERPROFILE%` 展开后的真实路径、内网 IP、疑似密钥串。
- 密钥只进本地配置、不进仓库；**备份文件同样含密钥，视为秘密**。

## 5. 验收：可复跑，不听自述

- 交付方必须给出**可复跑的命令与原始输出**。
- 验收方用**同样的命令独立复跑**——自测通过不算通过。
- 验收标准写在 `PROJECT_BLUEPRINT.md`，逐条对照。

## 6. 代码要干净（可验证）

- `uvx ruff check --select E,F,W,I,N,UP,B,SIM,RUF,TRY,PL,PERF,RET,ARG .` 零告警
- `uvx ruff format --check .` 通过
- 测试全绿；**不许用 `noqa` 掩盖**，**不许为过 lint 改动行为**。

## 8. 监督与复核（谁盯谁、怎么盯）

**交付方自报 ≠ 通过。** 监督是验收方的硬职责，不是礼貌动作。

- **每条声称都要对应一条可复跑命令。** 交活时给「声称 → 命令 → 原始输出」，缺一不可。
- **验收方必须逐条实跑**，包括对方已经跑过的——`self-reported success does not count`。
- **不许沉默改**：所有改动都要在回报里列出来；验收方抽查 diff，发现没报的改动即为**打回**。
- **任何形式的掩盖都算未通过**：`noqa`、配置文件豁免（如 `per-file-ignores`）、注释掉断言、
  把测试放宽到刚好通过、把验收命令改写成更容易过的版本。
- **打回必须给失败点**：具体命令 + 观察到的事实 + 期望值。含糊的"不行"无效。
- **被打回项要复验**：改完再跑同一条命令，直到通过或上报作者。
- **台账**：每轮验证记进 `VERIFICATION.md`（编号 / 声称 / 命令 / 实跑结果 / 结论）。
  被推翻的原声称也要留在台账里，不许抹掉。
- **监督范围包括验收方自己**：Hermes 的裁决与措辞同样接受质问（见 §3），错了照记。

## 9. 产物与落盘

- 工作目录即本项目目录；临时物用完即删。
- 会话日志（`*.log`）与往返 payload 不进仓库（见 `.gitignore`）。
- 破坏性命令先备份并打印备份路径。

## 10. 不许越界动别人的工具（作者定的红线）

**教训来源**：Hermes 为了让 `mimo run` 能跑，往 MiMoCode 的**全局配置**里写了一个 provider（含明文 apiKey），
又把 opencode 的凭据复制成 MiMoCode 的 `auth.json`。后果：**用户桌面版的模型列表被带偏**，
用户看到自己的工具"被改成 DeepSeek 了"。作者原话：**「他用他自己的模型，需要我会给他接其他的」**。

规矩：

- **不许替第三方工具配置 provider / 模型 / API key / 凭据。** 模型选什么是使用者的事。
- **不许把 A 应用的凭据复制给 B 应用**——跨应用共享 key 是越权。
- 需要对方工具具备某种能力时，**写进文档让使用者自己配**，不许代配；代码里做成"读使用者已有的配置"。
- 改第三方配置前，除了备份，**先说清会影响什么**——不是"能不能回滚"，而是"会不会改变它现在的行为"。
- **自己的验收结论不许建立在越界改动之上**：某个能力若依赖你私自加的东西，那条结论作废并标注。
